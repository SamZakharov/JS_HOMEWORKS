// First task
const arrowFunc1 = (a, b) => a + b;
console.log(arrowFunc1(1, 2));

// Second task
const arrowFunc2 = (str) => str.length;
console.log(arrowFunc2('Some string'));

// Third task
const arrowFunc3 = (arr) => arr.map((el) => el + 1);
console.log(arrowFunc3([1, 2, 3, 4]));
